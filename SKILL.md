---
name: project-workflow
description: 根据项目想法设计岗位分工和交付流程，经用户确认后在当前 Codex 项目中创建多个持久岗位任务并待命；明确开工后调度执行、验收与返工。适用于软件、内容、研究等项目的自动组队与协作管理；普通项目咨询或单项制作不自动创建团队。
---

# 项目团队工作流

将想法转为可执行的团队分工。岗位是当前 Codex 项目下的平级持久任务，不是应用原生父子任务，也不是临时 subagent。

## 先设计，确认后组队，开工后执行

区分三个授权：讨论方案、创建团队、执行项目。用户确认岗位方案仅授权创建并待命；只有明确“开始项目”或等价指令才执行业务。本技能的自动发现、引用、示例文本或普通想法描述都不构成创建授权。用户明确要求修改技能本身时，不执行组队流程。

1. 读取当前项目约定及相关现状；通过 list_projects、list_threads 确认项目身份和已有团队。不要要求用户重复提供可查询的信息。
2. 提炼目标、首批交付物、验收标准、约束和依赖。仅询问影响分工或成果的关键信息，其余写明合理假设。未确认前以会话展示方案，不创建任务。
3. 展示岗位表（岗位、职责边界、输入、交付物、验收责任人）及依赖流程、建议返工上限。每个团队一位项目经理；按交付物增加必要执行岗位和独立验收责任人，合并重复职责，不凑固定人数。简单项目不拆冗余岗位。
4. 请求用户确认该具体方案。用户若修改分工，更新方案后再确认；已明确确认的方案不反复询问。确认后读取 [编排协议](references/orchestration.md)，按两阶段机制创建团队。
5. 仅在全部岗位就绪后报告“团队已创建，等待开工”，列出实际创建的任务和职责。遵循 create_thread 工具要求输出创建任务的 UI 指令。明确告诉用户到项目经理任务发送“开始项目：目标”启动。

## 岗位选择

从交付物和风险推导岗位，而非从行业名机械套模板：

- 电商平台：项目经理、产品/体验设计、开发、测试是可能分工；前后端是否拆开取决于交付范围和可隔离性。支付联调等外部依赖单独标记，不能用模拟通过替代真实验收。
- 内容项目：项目经理、策划/研究、创作、编辑验收；是否需要视觉岗位取决于输出媒介。组队不授权发布。
- 研究项目：项目经理、研究/资料收集、分析、方法与来源审校；按数据和方法复杂度合并执行职责。资料不足应明确结论限制。

这些仅是思考示例。不要把示例当成用户需求或预先确定的岗位清单。

## 环境与归属

默认选当前任务所在的已保存项目，核实 projectId、hostId、真实路径和 isGitRepository。无项目则让用户选择已有项目或在界面添加；不要把普通目录创建说成已创建 Codex 项目，不默认改用无项目任务。多个候选无法辨认时再询问。

任务编号从工具返回值获得；标题只用于显示，不能作为唯一身份。重用必须匹配项目、workflow_id 和岗位映射，不能仅凭同名任务接管。现有项目规则和运行记录不覆盖；本团队使用独立 `.project-workflows/<workflow_id>/`。技能不改写全局配置或项目现有 AGENTS.md。

Git 项目遵循 create_thread 默认 worktree 策略；用户明确要求直接使用保存项目时才用 local。非 Git 项目用 local。不要指定用户未要求的模型、推理档位、分支或自动化。

## 团队命令

- 查看进度：读取状态、回执及必要的 wait_threads 快照，只汇报已核实进度。
- 开始项目：转给已登记的项目经理，由其确认团队 READY 和明确范围后派单。
- 暂停协作：经理停止新派单并请求运行岗位在安全边界停下；回执未齐前称 PAUSING，不谎称已经停止。
- 恢复协作：经理核实成果版本、未决交接和阻塞解除情况，只续接未完成工作。

执行与恢复时读取编排协议。角色消息使用 [启动模板](assets/role-prompts.md)，填入真实映射、路径和已批准职责；不保留未填字段。

## 完成界限

默认初次执行为第 0 轮，最多追加 3 轮返工；上限调整须有用户指令，不由团队自增。阻塞不等于通过，不擅自缩小验收标准。团队审批不自动授权购买、对外通信或发布；相应步骤需要用户针对该行为的授权。

任务消息与模型驱动协作依赖 Codex 和工具可用，不是常驻服务；本技能不默认设置周期唤醒。不可用时记录恢复点并说明，不能声称后台会自行继续。

示例调用：`使用 $project-workflow，我想做一个电商平台，请分析需要哪些岗位并给出协作方案。`
