# 岗位消息模板

组建者填入真实值后发送。方括号中的字段必须替换；本文件不是可直接派发的业务需求。

## 第一阶段：创建岗位任务

你是项目「[项目名]」团队中的「[岗位名]」。工作流身份为 [workflow_id]，role_id 为 [role_id]，唯一 role_key 为 [workflow_id:role_id]。
用户已确认创建本团队，但尚未授权开工。本轮仅初始化并待命，不开展研究、制作、代码修改、测试或向其他岗位派单。
控制目录：[control_root 绝对路径]。读取其中的 plan.md、team.json 和 protocol.md。你的职责：[批准的职责]；预期成果：[成果]；验收责任人：[角色]。
本轮只回复你已读取的职责及实际工作目录。角色映射可能尚未齐全，不猜测任务编号，不创建其他任务。等待后续配置就绪消息。

## 第二阶段：就绪检查

这是 [workflow_id] 的配置就绪检查，映射版本 [版本]。完整映射在 [team.json 绝对路径]，协议在 [协议绝对路径]，你是 [role_id]，真实任务编号 [threadId]，经理编号 [manager_thread_id]。
核实角色身份、控制目录访问、实际工作目录及成果交接方式，将结果写到 [本角色独立 ready 回执绝对路径]。仅写初始化回执并回复结果，仍然待命，不派发业务。后续遵循该协议；开工指令由用户明确发给经理。

## 开工后：经理派单

[workflow_id] / [run_id] / [work_item]，第 [round] 轮，handoff_id=[handoff_id]。你是 [role_id]。
已授权工作：[范围]。输入：[绝对路径及版本]。输出：[路径及交付要求]。验收标准：[具体标准]。
先核实最新状态、重复回执和成果版本。实际执行并保存验证证据；未执行的检查不得写通过。完成或阻塞后写 [回执绝对路径]，使用 send_message_to_thread 回传经理 [manager_thread_id]。不要自行唤醒其他执行者或扩展范围。

## 成员回传经理

交接 [handoff_id] 的结果为 [completed/blocked]。成果：[绝对路径与版本]。验证：[实际结论与证据路径]。问题或外部依赖：[内容或无]。请读取回执 [绝对路径]，按当前流程决定验收、下一步骤或返工。
